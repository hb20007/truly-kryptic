import '../../required-libs'
import { HallOfFameComponent } from "./hof";

describe('Hall-of-fame Component', () => {
    it('exists', () => {
        expect(HallOfFameComponent).toBeDefined();

        // todo - actual tests
    })
});

