import '../../required-libs'
import { AccountComponent } from "./account";

describe('Account Component', () => {
    it('exists', () => {
        expect(AccountComponent).toBeDefined();

        // todo - actual tests
    })
});

