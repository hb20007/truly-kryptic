import '../../test-utils';
import { LevelListComponent } from './level-list';

describe('Landing-page Component', () => {
    it('exists', () => {
        expect(LevelListComponent).toBeDefined();

        // todo - actual tests
    })
});

